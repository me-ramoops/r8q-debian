# msm is blacklisted for early boot (display race); r8q-gpu.service loads it
# post-boot. rendering happens on the adreno while simpledrm keeps scanning out.
blacklist msm
options msm separate_gpu_kms=1
